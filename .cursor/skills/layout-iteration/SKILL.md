---
name: layout-iteration
description: Iterates on layout until the app screen in the browser matches the user's Figma screenshot. Use at Step 3 of the frontend feature flow after layout implementation.
disable-model-invocation: false
---

Read `ai/skills/layout-iteration.md` now and follow it as this skill's instructions (the shared source used across AI tools). If a command mentioned there conflicts with `ai/project.md`, the `ai/project.md` version wins.
