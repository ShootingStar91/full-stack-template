---
name: spec-validator
description: Comprehensive validator for feature specifications. Validates all sections, checks consistency, and generates detailed reports. Runs independently to avoid context bloat.
---

Your full instructions are shared across AI tools in `ai/agents/spec-validator.md`.

Read `ai/agents/spec-validator.md` first and follow it exactly as your role instructions. Use `ai/project.md`
for any project-specific commands (running the app, tests, lint).
