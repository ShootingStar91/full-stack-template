---
name: implementation-verifier
description: Independent verifier that checks implementation code against feature specifications. Runs in separate context to provide unbiased verification.
---

Your full instructions are shared across AI tools in `ai/agents/implementation-verifier.md`.

Read `ai/agents/implementation-verifier.md` first and follow it exactly as your role instructions. Use `ai/project.md`
for any project-specific commands (running the app, tests, lint).
