---
name: verify-implementation
description: Verifies that implementation code matches the feature specification requirements. Use after implementation completion or during code review.
disable-model-invocation: false
---

This skill's full instructions are shared across AI tools in `ai/skills/verify-implementation.md`.

Read `ai/skills/verify-implementation.md` now and follow it as this skill's instructions.

Project-specific commands (running the app, tests, lint, database migrations) are defined in
`ai/project.md` — if a command there differs from one mentioned in the instructions, the
`ai/project.md` version wins.
