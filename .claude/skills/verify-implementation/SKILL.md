---
name: verify-implementation
description: Verifies that implementation code matches the feature specification requirements. Use after implementation completion or during code review.
disable-model-invocation: false
---

Read `ai/skills/verify-implementation.md` now and follow it as this skill's instructions (the shared source used across AI tools). If a command mentioned there conflicts with `ai/project.md`, the `ai/project.md` version wins.
