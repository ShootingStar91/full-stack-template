---
name: run-tests
description: Runs all tests (client and server unit tests, server integration/e2e tests, and Playwright E2E tests) using the Taito CLI. Use when the user asks to run tests, verify tests, or check test status.
disable-model-invocation: false
---

Read `ai/skills/run-tests.md` now and follow it as this skill's instructions (the shared source used across AI tools). If a command mentioned there conflicts with `ai/project.md`, the `ai/project.md` version wins.
