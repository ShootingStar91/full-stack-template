---
name: layout-iteration
description: Iterates on layout until the app screen in the browser matches the user's Figma screenshot. Use at Step 3 of the frontend feature flow after layout implementation.
disable-model-invocation: false
---

This skill's full instructions are shared across AI tools in `ai/skills/layout-iteration.md`.

Read `ai/skills/layout-iteration.md` now and follow it as this skill's instructions.

Project-specific commands (running the app, tests, lint, database migrations) are defined in
`ai/project.md` — if a command there differs from one mentioned in the instructions, the
`ai/project.md` version wins.
